import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 15,
    lineHeight: 22,
    flexShrink: 1,
  },
  bulletRow: {
    flexDirection: 'row',
    gap: 8,
    marginVertical: 2,
    alignItems: 'flex-start',
  },
  bullet: {
    fontSize: 15,
    lineHeight: 22,
    width: 16,
  },
});
